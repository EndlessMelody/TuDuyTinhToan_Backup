/**
 * AI Picks — Discover v3
 * ─────────────────────────────────────────────────────────────────
 * Cards composed by AIPicksSection (Fold 2 — GET READY).
 */
export { AIPickHeroCard } from "./AIPickHeroCard";
export type { AIPickHeroCardProps } from "./AIPickHeroCard";

export { AIPickDeckCard } from "./AIPickDeckCard";
export type { AIPickDeckCardProps } from "./AIPickDeckCard";
